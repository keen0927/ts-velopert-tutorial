let count: number = 0;
count += 1;
// count = '가나다라마바사';

const done: boolean = true;

const numbers: number[] = [1,2,3];
const message: string[] = ['Hello', 'Keen'];

message.push('1');

let mightBeUndefined: string | undefined = '2';
let nullableNumber: number | null = null;

let color: 'red' | 'orange' = 'red';
